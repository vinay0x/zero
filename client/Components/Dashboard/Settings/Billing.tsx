import Layout from '@components/Common/Layout';
import React, { ReactElement } from 'react';

interface Props {}

export default function Billing({}: Props): ReactElement {
  return <Layout title="Plans & Billing">Hey</Layout>;
}
