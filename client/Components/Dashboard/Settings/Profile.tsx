import Layout from '@components/Common/Layout';
import React, { ReactElement } from 'react';

interface Props {}

export default function ProfileSettings({}: Props): ReactElement {
  return <Layout title="Profile Settings">Hey</Layout>;
}
